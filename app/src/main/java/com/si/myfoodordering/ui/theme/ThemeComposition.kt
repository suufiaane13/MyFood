package com.si.myfoodordering.ui.theme

import androidx.compose.runtime.compositionLocalOf

/** Indique si l’utilisateur a activé le mode sombre (préférence persistée). */
val LocalAppDarkTheme = compositionLocalOf { false }
